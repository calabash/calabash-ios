*.gem
*.rbi
.config
coverage
InstalledFiles
pkg
vendor
spec/reports
test/tmp
test/version_tmp
tmp

# CI - occasionally we need to pull down run_loop to test
run_loop

# Documentation cache and generated files:
.yardoc
_yardoc
doc
rdoc

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
Gemfile.lock
.ruby-version
.ruby-gemset
.bundle

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# RubyMine
.idea

# notes
notes

# part of the gem
calabash-cucumber/staticlib

# MacOS
.DS_Store
__MACOSX

# emacs
(.*/)?\#[^/]*\#$
