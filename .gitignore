*.gem
*.rbi
.config
coverage
InstalledFiles
pkg
vendor
spec/reports
test/tmp
test/version_tmp
tmp
.irb-history
.pry-history

# These repos are cloned during CI, which we run locally to test.
run_loop
calabash-ios-server

# Documentation cache and generated files:
.yardoc
_yardoc
doc
!script/doc
rdoc

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
Gemfile.lock
.ruby-version
.ruby-gemset
.bundle

# rspec
calabash-cucumber/spec/reports

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# RubyMine
.idea

# PRIVATE - do not commit .env files
.env

# notes
notes

# libraries that are included in the gem
calabash-cucumber/staticlib
calabash-cucumber/dylibs

# MacOS
.DS_Store
__MACOSX

# emacs
(.*/)?\#[^/]*\#$

# vim
*.swp
